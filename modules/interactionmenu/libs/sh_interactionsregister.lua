----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------
lia.config.PlayerInteractionOptions = lia.config.PlayerInteractionOptions or {}
----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------
for name, vars in pairs(lia.config.PlayerInteractionOptions) do
    lia.playerInteract.addFunc(
        vars.name,
        {
            name = name,
            Callback = vars.Callback,
            CanSee = vars.CanSee,
        }
    )
end
----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------