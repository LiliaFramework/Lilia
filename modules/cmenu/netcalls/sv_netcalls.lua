----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------
netstream.Hook(
    "RunCMenuFunction",
    function(client, id, target)
        local func = lia.config.LoadOptions[id]
        if func then
            func(client, target)
        end
    end
)
----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------